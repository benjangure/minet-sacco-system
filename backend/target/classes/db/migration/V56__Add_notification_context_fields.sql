-- Add context fields to notifications table for personalized notifications
-- Columns already exist from previous migrations, this is a no-op migration

-- These columns were already added in earlier migrations:
-- loan_id, deposit_request_id, guarantor_id, category
-- This migration serves as a checkpoint in the migration history
