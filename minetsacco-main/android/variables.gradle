/**
 * Minet SACCO Android Build Variables
 * 
 * Google Play Store Requirements (2026):
 * - targetSdkVersion must be 34 or higher
 * - Current configuration: API Level 36 (Android 15) ✓
 */
ext {
    // SDK Versions - Play Store Compliant
    minSdkVersion = 24              // Android 7.0 (Minimum supported)
    compileSdkVersion = 36          // Android 15 (Latest stable)
    targetSdkVersion = 36           // Android 15 (Exceeds Play Store requirement)
    
    // AndroidX Library Versions
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    
    // Testing Library Versions
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    
    // Cordova Version
    cordovaAndroidVersion = '14.0.1'
    
    // Java Compatibility
    javaSourceCompatibility = JavaVersion.VERSION_17
    javaTargetCompatibility = JavaVersion.VERSION_17
}